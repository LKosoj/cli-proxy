"""Lint Evolution: self-evolving lint rules via classify→decide→feedback fractal.

Three levels:
  L1 (curator):       signals → fingerprints → candidates → active rules
  L2 (meta-curator):  notes → emergent fields → schema evolution
  L3 (regressor):     outcomes → logistic regression → updated weights
"""
