[pytest]
asyncio_default_fixture_loop_scope = function
markers =
    asyncio: async test marker (registered for environments without pytest-asyncio)
    ssh_integration: integration tests that start a local SSH server
